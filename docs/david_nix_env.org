


* Setup

  - This is flake.nix

#+BEGIN_SRC
                wireviz = pkgs.wireviz.overrideAttrs (old: {
                  # src = pkgs.fetchgit {
                  #   url = "file:///home/sjagoe/workspace/other/WireViz";
                  #   rev = "v0.4.1";
                  #   hash = "sha256-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=";
                  # };

                  src = pkgs.fetchFromGitHub {
                    owner = "davidjagoe";
                    repo = "WireViz";
                    rev = "refs/heads/master";
                    # When changing code, set hash to to force
                    # rebuild. The nix store is content addressed, so
                    # having an old hash will rebuild the old code
                    # despite a branch change/update.
                    # hash = "sha256-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=";
                    hash = "sha256-2topgXsrhB1+S7EbZP/p+hsJMeSINGaTM8qdSilGuCI=";
                  };
                });
#+END_SRC


* How to run wireviz in nix env
  - nix develop nixpkgs#wireviz
  - PYTHONPATH="$PYTHONPATH:$PWD/src" python -m wireviz.wv_cli examples/inner_shield_branch/Field_Wiring.yml



* If creating the gv file and post-processing

  The tweak section in the Field_Wiring.yml file sets packing, spacing
  and pagination. The last requires outputting the gv and then using
  dot which requires ghostscript command in the env, so this is
  required:

  - nix shell nixpkgs#ghostscript --command nix develop nixpkgs#wireviz
  - PYTHONPATH="$PYTHONPATH:$PWD/src" python -m wireviz.wv_cli -f g examples/inner_shield_branch/Field_Wiring.yml
  - dot -Tps2 -o harness.ps Field_Wiring.gv && ps2pdf harness.ps harness.pdf

* More stuff to split into multiple pdfs

  - nix shell nixpkgs#ghostscript --command nix develop nixpkgs#wireviz
  - PYTHONPATH="$PYTHONPATH:$PWD/src" python -m wireviz.wv_cli -f g examples/inner_shield_branch/Field_Wiring.yml
  - ccomps -x -o examples/inner_shield_branch/build/sheet.gv examples/inner_shield_branch/Field_Wiring.gv
  - python run.py # dot command creating the ps files
  - gs -dNOPAUSE -dBATCH -sDEVICE=pdfwrite -dDEVICEWIDTHPOINTS=1224 -dDEVICEHEIGHTPOINTS=792 -dFIXEDMEDIA -sOutputFile=examples/inner_shield_branch/field_wiring.pdf examples/inner_shield_branch/build/sheet*.ps
  - gs -dNOPAUSE -dBATCH -sDEVICE=pdfwrite -sOutputFile=examples/inner_shield_branch/field_wiring.pdf examples/inner_shield_branch/build/sheet*.ps
